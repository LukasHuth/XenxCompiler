use super::{
    Statement,
    Variable,
    ByteArray,
};

#[allow(unused_variables)]
pub fn genfor(expr: Statement, vars: &Vec<Variable>, used_positions: &Vec<usize>, highest_position: &usize, bytecode: &ByteArray)
{
    println!("");
}
